# hack/assets

Custom art and audio. Subfolders (created on demand):

- `sprites/overworld/cynthia/`  — Cynthia OW frames.
- `sprites/battle/`             — front/back sprites for added species.
- `tilesets/`                   — new map tiles.
- `music/`                      — `.s` files for new tracks.
- `_scratch/`                   — work-in-progress, gitignored.
